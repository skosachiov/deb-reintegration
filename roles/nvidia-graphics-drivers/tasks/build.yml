- name: Display build info (commands only)
  ansible.builtin.debug:
    msg:
      - "Extract package list: awk '/^Package-List:/{flag=1; next} /^[^ ]/{flag=0} flag && NF>=2 {print $1}' \"{{ build_dir }}/{{ dsc_filename }}\""
      - "Install build dependencies: yes | mk-build-deps -i -r (in {{ source_dir }})"
      - "Build packages: dpkg-buildpackage -us -uc -b (in {{ source_dir }})"
